# 模块文档索引

按代码路径 / 产品区域列出这个仓库现有的模块文档。命中多个就都读；索引里没有命中的模块，按
`docs/` 未覆盖处理，不要为了「有文档」新建近乎空白的条目（见 `references/docs-tree.md` §6）。

| 模块 | 路径 | 覆盖什么 |
|---|---|---|
| BambooHR MCP 工具 | `docs/modules/bamboohr-mcp.md` | `src/bamboohr_mcp/` 下全部 MCP 工具清单、凭据/多租户模型、已知未验证假设 |
